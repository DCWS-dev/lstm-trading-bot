# 🚀 Paper Trading System - Quick Start Guide

## ⚡ Самый быстрый способ запустить всё

```bash
bash SIMPLE-START.sh
```

Вот и всё! ✨ Одна команда запускает:
- ✅ Python окружение
- ✅ Проверку всех 29 моделей ML
- ✅ Проверку 870k свечей OHLCV
- ✅ Node.js сервер с ботом
- ✅ Открывает дашборд для мониторинга

## 📊 Что произойдет

```
┌─────────────────────────────────────┐
│ bash RUN.sh                         │
├─────────────────────────────────────┤
│ [1/4] Активирует Python окружение  │
│ [2/4] Валидирует 29 моделей        │
│ [3/4] Очищает порт 3000            │
│ [4/4] Запускает ботa               │
└─────────────────────────────────────┘
              ↓
   📊 Dashboard готов
   🤖 Бот торгует
   💰 Логирует сделки
```

## 🌐 Где найти дашборд

После запуска откроется:
```
http://localhost:3000/trading-dashboard.html
```

### Что видишь на дашборде:
- 📈 **Win Rate** - текущий процент прибыльных сделок
- 💰 **Portfolio Value** - стоимость портфеля в реальном времени
- 📊 **Summary Stats** - 8 ключевых метрик
- 🎯 **Active Pairs** - 29 торговых пар со статусом
- 📋 **Recent Trades** - последние 20 сделок
- 📥 **CSV Export** - экспорт данных

## 🎯 Первая неделя (Week 1)

### Цели:
- [ ] Собрать 100+ сделок
- [ ] Достичь 44-48% Win Rate
- [ ] Подтвердить базовую производительность
- [ ] Мониторить без ошибок 7 дней

### Процесс:
1. Запустить `bash RUN.sh`
2. Открыть дашборд в браузере
3. Наблюдать в течение 7 дней
4. Дашборд обновляется каждые 2 секунды

## 🛑 Как остановить систему

```bash
# Остановить в терминале
Ctrl + C

# Или если нужно быстро:
pkill -f "node start.js"
```

## 📁 Файловая структура

```
├── RUN.sh                           ← ГЛАВНАЯ КОМАНДА 🚀
├── START.sh                         ← Альтернатива
├── src/
│   └── paper-trading-bot.js         ← Торговый бот
├── ml/
│   ├── models/                      ← 29 XGBoost моделей
│   └── *_router.py                  ← Маршрутизатор сигналов
├── public/
│   └── trading-dashboard.html       ← Дашборд (отправляется на порт 3000)
├── logs/
│   ├── *.csv                        ← Исторические OHLCV данные
│   └── paper-trading/               ← Логи каждой сделки
└── config/
    └── live-config.json             ← Настройки бота
```

## 📂 Доступные скрипты

| Скрипт | Назначение | Когда использовать |
|--------|-----------|-------------------|
| `bash SIMPLE-START.sh` | **Основной** - просто начни отсюда | ✅ Всегда используй это |
| `bash RUN.sh` | Полнофункциональный вариант | Опционально, если нужен расширенный функционал |
| `bash START.sh` | Альтернатива RUN.sh | Если RUN.sh не работает |

**Рекомендация:** Используй `SIMPLE-START.sh` - это самый надежный и простой способ.

## ⚙️ Параметры, которые можно менять

### В `config/live-config.json`:
```json
{
  "initialCapital": 10000,           // Начальный капитал
  "positionSize": 300,               // Размер позиции на сделку ($)
  "minConfidence": 0.502,            // Минимальная confidence для сигнала
  "pairs": [29 crypto pairs],        // Какие пары торговать
  "updateInterval": 1000             // Интервал обновления (мс)
}
```

## 🔍 Как проверить логи

```bash
# Последние 50 сделок
tail -50 logs/paper-trading/*.json

# Все сделки за сегодня
ls -lt logs/paper-trading/ | head -1

# Общая статистика
cat logs/paper-trading/*.json | jq '.summary'
```

## 📈 Ожидаемые результаты

### Week 1 (Валидация):
```
✓ Win Rate: 44-48%
✓ Trades: 100+
✓ Daily ROI: 0.5-2%
✓ Sharpe: 1.5-2.0
```

### Week 2 (Адаптивная маршрутизация):
```
✓ Win Rate: 50-51% (+1-3%)
✓ Улучшение за счет режима рынка
✓ Лучший timing входа/выхода
```

### Week 3+ (Live Trading):
```
✓ Deploy с $100 на сделку
✓ Масштабирование ежемесячно
✓ Target: 52-55% WR к концу месяца
```

## 🐛 Решение типичных проблем

### ❌ "Port 3000 already in use"
```bash
# Убить старый процесс
lsof -i :3000 | grep node | awk '{print $2}' | xargs kill -9
```

### ❌ "Python not found"
```bash
# Пересоздать venv
rm -rf venv
bash RUN.sh
```

### ❌ "Models not loading"
```bash
# Проверить модели
python3 -c "
import os
models = [f for f in os.listdir('ml/models') if f.endswith('.joblib')]
print(f'Found {len(models)} models')
"
```

### ❌ "Dashboard won't load"
```bash
# Проверить сервер
curl http://localhost:3000/trading-dashboard.html | head -20
```

## 🚀 Advanced: Запуск в фоне

```bash
# Запустить и отключиться от терминала
nohup bash RUN.sh > trading.log 2>&1 &

# Смотреть логи вживую
tail -f trading.log

# Найти процесс
ps aux | grep RUN.sh
```

## 📞 Контрольный список перед запуском

- [ ] Python 3.9+ установлен (`python3 --version`)
- [ ] Node.js установлен (`node --version`)
- [ ] 29 ML моделей в `ml/models/`
- [ ] 29 CSV файлов в `logs/`
- [ ] `public/trading-dashboard.html` существует
- [ ] Порт 3000 свободен

Если всё ✅, просто запусти:

```bash
bash RUN.sh
```

И всё заработает! 🎉

---

**Made with ❤️ for consistent 48%+ Win Rate Trading**

